extends ../../layout/default.pug
include ../../mixins/select-tree.pug
block main
  include ../../mixins/alert.pug
  +alert-error(messages.error)
  h1 Chi tiết nhóm quyền
  form(action=`${prefixAdmin}/roles/edit/${role.id}?_method=PATCH` method="POST" )
    .mb-3
      label.form-label Tiêu đề
      input.form-control(type="text" value=role.title name="title" placeholder="Nhập tiêu đề nhóm quyền" required readonly)
    .mb-3 
      label.form-label Mô tả
      textarea.form-control(name="description" id='' placeholder="Nhập mô tả nhóm quyền" readonly)= role.description
    a.btn.btn-success(href=`${prefixAdmin}/roles/edit/${role.id}`) Chỉnh sửa
  script(src='/admin/js/product.js')