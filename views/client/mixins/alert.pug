mixin alert-success(message)
  - if(messages.success)
      div(data-server-alert data-type="success" data-message=messages.success class="hidden")
mixin alert-error(message)
  - if(messages.error)
      div(data-server-alert data-type="error" data-message=messages.error class="hidden")